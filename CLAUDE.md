# Claude instructions

For installation, configuration, repair, hardware testing, upgrades, or removal, read and follow `.agents/skills/install-codex-gamepad/SKILL.md` in full. The skill is plain Markdown and applies even when the client does not provide native Codex skill loading.

Also follow `AGENTS.md`. Preserve unrelated files and Karabiner profiles, keep Codex response text out of logs and terminal output, let the user enter passwords or grant macOS permissions locally, and distinguish software checks from physical controller verification.
